import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: "bold",
    color: "white",
  },
  poster: {
    width: 150,
    height: 240,
    borderRadius: 7,
    margin: 5,
    resizeMode: "cover",
  },
});

export default styles;
